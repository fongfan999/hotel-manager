<%= simple_form_for room do |f| %>
	<%= f.input :code, label: "ID" %>
	<%= f.input :name %>
	<%= f.association :type %>
	<%= f.input :annotation %>

	<%= f.button :submit, class: "btn-success" %>
<% end %>