<!DOCTYPE html>
<html>
<head>
  <title><%= content_for?(:title) ? yield(:title) : "ABC Hotel" %></title>
  <%= stylesheet_link_tag    'application', :media => 'all' %>
  <%= javascript_include_tag "https://www.google.com/jsapi" %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body>
	
<nav class="navbar navbar-default">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand topnav" href="/">ABC Hotel</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      
    <ul class="nav navbar-nav">
          <li class=<%= "active" if current_page?("/") %>>
            <%= link_to "Home", root_path %>
          </li>
          <li class=<%= "active" if current_page?("rooms") %>>
            <%= link_to "Room", rooms_path %>
          </li>
          <li class=<%= "active" if current_page?("bills") %>>
            <%= link_to "Bill", bills_path %>
          </li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <li class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#">Admin
            <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Services", services_path %></li>
              <li class=<%= "active" if current_page?("customers") %>>
                <%= link_to "Customer", customers_path %>
              </li>
              <li class=<%= "active" if current_page?("receipts") %>>
                <%= link_to "Receipt", receipts_path %>
              </li>
            </ul>
          </li>
          <% if user_signed_in? %>
          <li class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#">
              Account
            <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li class="navbar-text page-header">
                Signed in as <%= current_user.email %>
              </li>
              <li>
                <%= link_to "Edit", edit_user_registration_path %>
              </li>
              <li>
                  <%= link_to "Log out", destroy_user_session_path,
                    method: :delete %>
              </li>
            </ul>
          </li>
          <% else %>
            <li class=<%= "active" if current_page?("users/sign_in") %>>
              <%= link_to "Sign in", new_user_session_path %>
            </li>
          <% end %>
        </ul>

    </div><!-- /.navbar-collapse -->
  </div>
</nav>

<div class="container">
  <% unless current_page?("/") %>
    <% flash.each do |key, message| %>
    <div class="alert alert-<%= key %> fade in">
      <a href="#" class="close" data-dismiss="alert" aria-label="close">
        &times;
      </a>
      <%= message %>
      </div>
    <% end %>
  <% end %>
</div>

<div class=<%= "container" unless current_page?("/") %>>
	<%= yield %>
</div>

<div id="footer">
  <div class="container">
    <div class="row">
      <div class="col-sm-3">
        <h4>ADDRESS</h4>
        <span class="border-top-info"></span>
        <p>5046 S Greenwood Avenue, Chicago, IL 60615</p>
      </div>
      <div class="col-sm-3">
        <h4>CONTACT</h4>
        <span class="border-top-info"></span>
        <p>
          info@abchotel.com
        </p>
        <p>
          Tel: 123-456-789
        </p>
      </div>
      <div class="col-sm-3">
        <h4>ABOUT US</h4>
        <span class="border-top-info"></span>
        <p>Alphabet Inc. is an American multinational conglomerate created in 2015 as the parent company of Google and several other companies previously owned by Google.</p>
      </div>
      <div class="col-sm-3">
        <h4>WE ACCEPT</h4>
        <span class="border-top-info"></span>
        <p>
          <span style="color: #F8A211;">
            <i class="fa fa-cc-visa fa-2x"></i>
          </span>
          <span style="color: #FF0018;">
            <i class="fa fa-cc-mastercard fa-2x "></i>
          </span>
          <span style="color: #C09B43;">
            <i class="fa fa-cc-amex fa-2x"></i>
          </span>
          <span style="color: #3BA134;">
            <i class="fa fa-cc-jcb fa-2x"></i>
          </span>
          <span style="color: #002F86;">
            <i class="fa fa-cc-paypal fa-2x"></i>
          </span>
          <span style="color: #A2C2CD;">
            <i class="fa fa-cc-discover fa-2x"></i>
          </span>
          <span style="color: #0370CD;">
            <i class="fa fa-cc-dinners-club fa-2x"></i>
          </span>
        </p>
      </div>
    </div>
  </div>
</div>
</body>
</html>
