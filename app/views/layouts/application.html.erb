<!DOCTYPE html>
<html>
<head>
  <title><%= content_for?(:title) ? yield(:title) : "ABC Hotel" %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag    'application', :media => 'all' %>
  <%= javascript_include_tag "https://www.google.com/jsapi" %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body ng-app="myApp">	

<nav class="navbar navbar-inverse navbar-fixed-top ">
    <div class="container">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <%= link_to image_tag("logo.png"), root_path %>
      </div>
      
      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li class=<%= "active" if current_page?("/") %>>
            <%= link_to "Home", root_path %>
          </li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <% if user_signed_in? %>
            <% if current_user.admin? || current_user.employee? %>
              <li class=<%= "active" if current_page?("/receipts") || current_page?("/bills") || current_page?("/services") %>>
                <a class="dropdown-toggle" data-toggle="dropdown" href="#">Actions
                <span class="caret"></span></a>
                <ul class="dropdown-menu">
                  <li><%= link_to "Receipts", receipts_path %></li>
                  <li><%= link_to "Bills", bills_path %></li>
                  <li><%= link_to "Services", services_path %></li>
                  <% if current_user.admin? %>
                  <li><%= link_to "Statistics", admin_statistics_path %></li>
                  <li><%= link_to "Room Types", admin_room_types_path %></li>
                  <% end %>
                </ul>
              </li>
            <% end %>
            
            <li class=<%= "active" if current_page?("/users/edit") %>>
              <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                Account
              <span class="caret"></span>
              </a>
              <ul class="dropdown-menu">
                <li class="navbar-text page-header">
                  Signed in as <%= current_user.email %>
                </li>
                <% if current_user.admin? %>
                  <li><%= link_to "Dashboard", admin_root_path %></li>
                <% else %>
                  <% unless current_user.employee? || current_user.customer.nil? %>
                    <li><%= link_to "My Profile", customer_path(current_user.customer) %></li>
                  <% end %>
                <% end %>
                <li>
                  <%= link_to "Edit", edit_user_registration_path %>
                </li>
                <li>
                    <%= link_to "Log out", destroy_user_session_path,
                      method: :delete %>
                </li>
              </ul>
            </li>
          <% else %>
            <li class="<%= "active" if current_page?("/users/sign_up") %>">
              <%= link_to "Sign up", new_user_registration_path %>
            </li>
            <li class="<%= "active" if current_page?("/users/sign_in") %>">
              <%= link_to "Sign in", new_user_session_path %>
            </li>
          <% end %>
        </ul>
      </div>
      <!-- /.navbar-collapse -->
    </div>
    <!-- /.container -->
  </nav>
  

<div class="container">
  <% unless current_page?("/") %>
    <% flash.each do |key, message| %>
    <div class="alert alert-<%= key %> fade in">
      <a href="#" class="close" data-dismiss="alert" aria-label="close">
        &times;
      </a>
      <%= message %>
      </div>
    <% end %>
  <% end %>
</div>

<div class=<%= "container" unless current_page?("/") || current_page?("/admin") %>>
	<%= yield %>
</div>

<% unless current_page?("/") %>
<div id="footer">
  <div class="container">
    <div class="row">
      <div class="col-sm-3">
        <h4>ADDRESS</h4>
        <span class="border-top-info"></span>
        <p>5046 S Greenwood Avenue, Chicago, IL 60615</p>
      </div>
      <div class="col-sm-3">
        <h4>CONTACT</h4>
        <span class="border-top-info"></span>
        <p>
          info@abchotel.com
        </p>
        <p>
          Tel: 123-456-789
        </p>
      </div>
      <div class="col-sm-3">
        <h4>ABOUT US</h4>
        <span class="border-top-info"></span>
        <p>Alphabet Inc. is an American multinational conglomerate created in 2015 as the parent company of Google and several other companies previously owned by Google.</p>
      </div>
      <div class="col-sm-3">
        <h4>WE ACCEPT</h4>
        <span class="border-top-info"></span>
        <p>
          <span style="color: #F8A211;">
            <i class="fa fa-cc-visa fa-2x"></i>
          </span>
          <span style="color: #FF0018;">
            <i class="fa fa-cc-mastercard fa-2x "></i>
          </span>
          <span style="color: #C09B43;">
            <i class="fa fa-cc-amex fa-2x"></i>
          </span>
          <span style="color: #3BA134;">
            <i class="fa fa-cc-jcb fa-2x"></i>
          </span>
          <span style="color: #002F86;">
            <i class="fa fa-cc-paypal fa-2x"></i>
          </span>
          <span style="color: #A2C2CD;">
            <i class="fa fa-cc-discover fa-2x"></i>
          </span>
          <span style="color: #0370CD;">
            <i class="fa fa-cc-dinners-club fa-2x"></i>
          </span>
        </p>
      </div>
    </div>
  </div>
</div>
<% end %>
</body>
</html>
