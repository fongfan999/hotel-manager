<%= title("Chỉnh sửa tài khoản") %>

<header>
  <h2>Chỉnh sửa tài khoản</h2>
</header>

<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :email, required: true, autofocus: true %>

    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <p>Currently waiting confirmation for: <%= resource.unconfirmed_email %></p>
    <% end %>

    <%= f.input :password, autocomplete: "off", hint: "leave it blank if you don't want to change it", required: false, label: "Mật khẩu mới" %>
    <%= f.input :password_confirmation, required: false, label: "Xác nhận mật khẩu mới" %>
    <%= f.input :current_password, hint: "Chúng tôi cần Mật khẩu hiện tại của bạn để xác nhận thay đổi", required: true, label: "Mật khẩu hiện tại" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Cập nhật", class: "btn-success" %>
  </div>
<% end %>

<%#= link_to "Cancel my account", registration_path(resource_name), data: confirm: "Are you sure?" }, method: :delete %></p>

<%= link_to "Trở về", :back, class: "back" %>
