<%= simple_form_for customer do |f| %>
	<%= f.input :name %>
	<%= f.input :identity_card %>
	<%= f.association :type, label_method: :to_label, include_blank: false %>
	<%= f.input :address %>

	<%= f.button :submit, class: "btn-success" %>
<% end %>