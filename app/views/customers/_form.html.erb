<%= simple_form_for customer do |f| %>
	<%= f.input :name %>
	<%= f.input :identity_card %>
	<%= f.association :type %>
	<%= f.input :address %>

	<%= f.button :submit, class: "btn-success" %>
<% end %>